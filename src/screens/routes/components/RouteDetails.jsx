import React from "react";

const RouteDetails = ({ title }) => {
  return (
    <div className="route-details">
      <h1 className="routes-details-title">{title}</h1>
    </div>
  );
};

export default RouteDetails;
